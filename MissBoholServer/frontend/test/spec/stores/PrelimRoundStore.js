'use strict';

describe('PrelimRoundStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/PrelimRoundStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
