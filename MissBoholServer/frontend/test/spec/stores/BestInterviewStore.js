'use strict';

describe('BestInterviewStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/BestInterviewStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
