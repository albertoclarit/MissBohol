'use strict';

describe('BestSwimsuitStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/BestSwimsuitStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
