'use strict';

describe('FinalRoundStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/FinalRoundStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
