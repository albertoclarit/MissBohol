'use strict';

describe('JudgeActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/JudgeActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
