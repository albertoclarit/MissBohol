'use strict';

describe('PrelimRoundActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/PrelimRoundActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
