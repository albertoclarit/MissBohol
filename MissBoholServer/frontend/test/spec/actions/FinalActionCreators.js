'use strict';

describe('FinalActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/FinalActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
