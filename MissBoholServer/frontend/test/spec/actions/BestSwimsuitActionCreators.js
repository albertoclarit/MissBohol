'use strict';

describe('BestSwimsuitActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/BestSwimsuitActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
