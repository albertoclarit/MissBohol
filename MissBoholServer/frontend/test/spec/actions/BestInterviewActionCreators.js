'use strict';

describe('BestInterviewActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/BestInterviewActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
