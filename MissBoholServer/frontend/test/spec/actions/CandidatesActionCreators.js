'use strict';

describe('CandidatesActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/CandidatesActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
