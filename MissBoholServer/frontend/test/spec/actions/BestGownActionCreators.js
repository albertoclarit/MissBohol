'use strict';

describe('BestGownActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/BestGownActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
