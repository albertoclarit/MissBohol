'use strict';

describe('LoginActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/LoginActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
