'use strict';

describe('FinalRoundActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/FinalRoundActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
